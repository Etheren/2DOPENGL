This is a Simple 2D scene being Rendered in C++ using some OpenGL Libraries.

Idea is to design a simple house in a field, with a tree that when clicked on, drops an apple.

You'll need to dowload the Common Repository alongside this one for the program to be able to run.
